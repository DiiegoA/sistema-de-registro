// Restablecer un commit
git restore --source codigo del comimit(eb2c39b) nombre del archivo(index.html)

// Crear una rama
git checkout -b nombre de la rama(desarrollo)

// Ubicarme en una rama cualquiera
git switch rama(main)

// Para actualizar una rama debemos pararnos en la rama a actualizar
git merge rama que quiero traer la actualizacion(desarrollo)

// Para actualizar el repo desde otra rama que no sea main
git push origin rama(desarrollo)
